// Callback function 
// definaton of callback 
// when the code excute after sometimes whic is caries a normal function . when its excute after this function called callback function 
function callback(data) {
    console.log('callback');
    }
    // Create a new instance of the client with your API key and secret, then call `client.get()` to make an HTTP request:


    // HOF 
    // which function accept another function or return another function its called HOF
    const add = (a,b)=>{return a+ b;}

    // constructor function 
    // when normal function is used "this" keyword and when we call this function there used a "new" keyword as lke fuction is constructer function . its used make same propertis many item.
    let obj1 = new Person("Rahul",25);
    obj1.greet();


