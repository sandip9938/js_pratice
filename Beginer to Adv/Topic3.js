// ---------------STRING INDEXING--------------------
 let name = "sandip"
 console.log(name.length);
 console.log(name.toUpperCase());
 console.log(name.toLowerCase());
 console.log(name[0]);//gives first character of the string
 console.log(name[-1])//undefined because there is no negative index in array or string
 console.log(name[5]);//undefined because we are trying to access an element which does not exist
 //accessing elements using positive index
 console.log(name.typeof);


 let str = +"sand"
 console.log(str.typeof);
 
