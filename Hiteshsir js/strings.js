const name = "sandip"
const age = 22
console.log( name + age )
console.log(`my nae is ${name} and my ${age} years old.`);

// prototypes
console.log(name.length);
console.log(name.toUpperCase());
// as like many methods in the strings 
// , we can also use it on numbers too!
let num1=5; let num2=60 ; console.log(`${num1+num2}`);

