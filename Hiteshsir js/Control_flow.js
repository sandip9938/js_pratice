// if 
if(condition=true){
    // do something;
}
else{
    //do other thing.
    }
    // <,>,<=,>=,==,!=,===,!===
 



    // if else 
    balance=800
 if(balance<500){
console.log("balace is less than 750");
 }else if(balance<900){
    console.log("less than 1200");
 }
 else{
    console.log('Empty');
 }





// Switch Case

    // - **switch** statement:
    // It is used to compare a value with multiple values and perform different actions based on the comparison result.


    // Switch statements are used to perform different actions based on a value or expression. The switch block consists of one or more case blocks, each containing a


    // Switch statements are used to perform different actions based on the value of a variable or expression. The switch keyword is followed by an expression, enclosed

    // * switch case statements are used to perform different actions based on the value of a variable or expression.

    // Switch statements are used to perform different actions based on a specific condition. The switch case is executed when the value of an expression matches one or more

    // Switch statements are used to execute a block of code based on different conditions. The switch keyword is followed by an expression that evaluates into one or more

    // - It is used to compare a value with multiple values and execute code based on the comparison result.
   
switch (key) {
    case value:
        
        break;

    default:
        break;
}

